<section class="banner h-[90vh] flex items-center justify-center text-center bg-cover bg-center bg-no-repeat" id="home">
  <div class="container mx-auto px-6">
    <div class="w-full text-container" data-aos="fade-in">
      <h1 class="text-white text-5xl font-bold animate-slide-up">Hi! I'm Sychonix</h1>
      <h4 class="text-white text-lg mt-4 animate-slide-up">is an Independent node validator.</h4>
      <br />
      <i class="text-gray-300 animate-slide-up">
        <q>As an independent validator, Sychonix is committed to maintaining the stability and security of blockchain networks through reliable and efficient node operations. We support the development of a trusted, transparent, and innovative blockchain ecosystem, with a focus on tangible contributions in maintaining network sustainability.</q>
      </i>
    </div>
  </div>
</section>

<style>
/* Atur background gambar */
.banner {
  background-image: url('/bg-main-dark-1440w.jpg');
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  height: 90vh; /* Tinggi untuk desktop */
}

/* Animasi slide up */
@keyframes slideUp {
  from {
    transform: translateY(50px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.animate-slide-up {
  animation: slideUp 1.5s ease-out forwards;
}

/* Atur posisi teks di tengah */
.text-container {
  max-width: 800px;
  margin: 0 auto;
  transform: translateY(-2rem); /* Mendorong teks lebih ke atas untuk desktop */
}

/* Responsif untuk layar kecil */
@media (max-width: 768px) {
  .banner {
    height: 85vh; /* Kurangi tinggi banner untuk layar kecil */
  }

  .text-container {
    transform: translateY(-4rem); /* Geser teks lebih ke atas untuk layar kecil */
  }

  .text-container h1 {
    font-size: 2rem;
  }

  .text-container h4 {
    font-size: 1rem;
    margin-top: 0.5rem;
  }

  .text-container i {
    font-size: 0.875rem;
    line-height: 1.4;
    display: block;
    margin-top: 1rem;
  }
}
</style>
